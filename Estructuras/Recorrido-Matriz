// Recorrer matriz
// Sirve para mostrar o procesar matrices

for (int i = 0; i < filas; i++)
{
    for (int j = 0; j < columnas; j++)
    {
        printf("%d ", matriz[i][j]);
    }

    printf("\n");
}
