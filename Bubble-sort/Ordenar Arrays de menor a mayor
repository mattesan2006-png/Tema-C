// Bubble Sort
// Sirve para ordenar arrays de menor a mayor

for (i = 0; i < n - 1; i++)
{
    for (j = 0; j < n - 1 - i; j++)
    {
        if (v[j] > v[j + 1])
        {
            temp = v[j];
            v[j] = v[j + 1];
            v[j + 1] = temp;
        }
    }
}
